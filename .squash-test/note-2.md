Second commit on the squash-test branch.

Two commits means the squash-merge result will be a single commit on main
combining both — a useful contrast to seeing both commits land verbatim
under merge-commit / rebase semantics.
